syntax = "proto3";

package com.variflight.middleware.gateway.cache;

message ChangeMeta{
    bytes fieldTags = 1;
    repeated ChangeMeta fields = 2;
    map<int32, ChangeMeta> map_int32 = 3;
    map<int64, ChangeMeta> map_int64 = 4;
    map<bool, ChangeMeta> map_bool = 5;
    map<string, ChangeMeta> map_string = 6;
}